from typing import ClassVar

import numpy as np  # noqa: F401  # реэкспортируется в дочерние планировщики


class Scheduler:
    """
    Класс Scheduler - Планировщик скорости обучения

    Является базовым классом для всех планировщиков.
    Каждый планировщик обязан наследоваться от этого класса,
    а также реализовать метод lr.

    Планировщик не хранит состояния: метод lr по номеру шага step
    (нумерация с нуля), полному числу шагов total_steps и базовой
    скорости обучения base_lr возвращает скорость обучения для этого
    шага. GUI подставляет её в параметр lr оптимизатора перед каждым
    шагом; у оптимизаторов без параметра lr расписание не применяется.

    Возвращаемое значение должно линейно масштабироваться по base_lr
    (lr = base_lr * множитель(step)), поэтому нижние границы задаются
    долями от base_lr, а не абсолютными значениями.

    Дочерний планировщик описывает смысл своих параметров в словаре
    param_descriptions (ключи совпадают с params) — GUI показывает
    эти описания как подсказки к полям ввода.
    """

    param_descriptions: ClassVar[dict[str, str]] = {}

    def __init__(self, params: dict[str, float]) -> None:
        self.params = params

    def lr(self, step: int, total_steps: int, base_lr: float) -> float:
        """
        Метод lr, в котором рассчитывается скорость обучения для шага step.
        Каждый дочерний планировщик обязан реализовать этот метод
        """

        raise NotImplementedError
